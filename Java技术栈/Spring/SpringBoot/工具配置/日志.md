application.yml

```java
logging:
  pattern:
    #设置控制台打印格式
    console:
  file:
    name: logs/log.log
  logback:
		#滚动日志，超过100MB就新增
    rollingpolicy:
      maxFileSize: 100MB
      fileNamePattern: logs/log.%d{yyyy-MM-dd}.%i.log

```

